---
output:
  word_document:
    keep_md: yes
---
### 介绍
计算 BMI

### 代码
```{r}
BMI <- function(x, y) {
  # 输入体重，身高，其中x为体重，y为身高
  s <- c(x / (y ^ 2))           # 计算出 BMI 值，并赋给 s
  if (s >= 32)
    "哎，就这样自暴自弃吧"      # 如果 BMI 值大于等于 32，则为非常肥胖
  if (s < 32 & s >= 28)
    "北鼻，要努力减肥哦！"      # 如果BMI值在28-32之间，则为肥胖
  if (s < 28 & s >= 25)
    "北鼻，要适当控制体重咯！"  # 如果 BMI 值在 25 - 28 之间，则为轻度肥胖
  if (s < 25 & s >= 18.5)
    "北鼻，你的身材太好了啊！"  # 如果 BMI 值在 18.5 - 25 之间，则为标准身材
  if (s < 18.5)
    "太瘦了也不好哦！"          # 如果 BMI 值在 18.5 以下，则为过轻
}
  # 可以将上程序积累的数据用下列程序绘制直方图
hist(data1, xlab = "day", ylab = "BMI", freq = FALSE, cex.axis = 0.7, cex.lab = 0.7)
curve(dnorm(x, mean(data1), sd(data1)), add = T, col = "blue", lwd = 2)
# 组合起来可以作为简单的体重监控程序
```

### 演示
```{r}

```

